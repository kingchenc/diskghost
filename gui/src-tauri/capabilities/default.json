{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access (event listen for live progress, window) + dialog for the main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
